# Pinned for reproducibility (was all >=, breaking on TF/numpy2 combos)
# Requires Python >=3.10 (uses dict[str, ...] syntax)
#
# Apple Silicon (arm64 macOS) cannot install stock `tensorflow` wheels -
# use `tensorflow-macos` + `tensorflow-metal` instead. The markers below
# select the right package automatically.
pandas==2.2.0
numpy==1.26.4
scikit-learn==1.3.2
tensorflow==2.15.0; platform_machine != "arm64" or platform_system != "Darwin"
tensorflow-macos==2.15.0; platform_machine == "arm64" and platform_system == "Darwin"
tensorflow-metal==1.1.0; platform_machine == "arm64" and platform_system == "Darwin"
matplotlib==3.8.2
seaborn==0.13.0
python-dateutil==2.8.2
